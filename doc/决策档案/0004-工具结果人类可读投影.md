# 0004 — 工具结果人类可读投影

## 决定

`i18n_get_locale` 与本扩展重注册的工具一样，默认显示紧凑的人类可读状态，以 `keyHint`（实际按键提示）说明如何展开；展开显示解析来源、fallback（回退语言）和内置 UI 语言包。工具不直接展示 `details` 或 JSON；结构化副本仅供模型和运行时使用。

## 后果

任何新增公开工具都必须提供同样的 `renderResult`；展开层必须增加相对折叠层有诊断价值的信息。测试覆盖折叠、展开和执行中的结果，避免回退到宿主默认对象渲染。
